# Test fixture

A fictional persona used to run the skill's test cases without touching the
user's real profile. The voice here is deliberately distinctive - no emoji, no
hashtags, British spelling, long paragraphs, flat closings, a recurring "Anyway"
- so that voice-matching can be checked objectively rather than by vibes.

To run an eval, point the skill's `profile/` directory at `fixtures/profile/`.
